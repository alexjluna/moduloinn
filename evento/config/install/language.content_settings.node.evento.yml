#language.content_settings.node.evento.yml

langcode: es
status: true
dependencies:
  config:
    - node.type.evento
id: node.evento
target_entity_type_id: node
target_bundle: evento
default_langcode: site_default
language_alterable: false
